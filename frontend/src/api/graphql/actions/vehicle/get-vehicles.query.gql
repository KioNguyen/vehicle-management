query GetListVehicle ($page: Int, $limit: Int) {
    getListVehicle(pagination: { page: $page, limit: $limit }) {
        items {
            id
            name
            brand
            description
            bodyType
            fuelType
            price
            createdAt
            updatedAt
            deletedAt
        }
        pagination {
            total
            page
            limit
            totalPage
        }
    }
}
